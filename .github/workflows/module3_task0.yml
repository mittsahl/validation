name: Module3_task0
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
            -run: git clone https://github.com/mittsahl/validation && cd validation/module3_task0
            -run: make help
